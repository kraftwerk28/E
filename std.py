functions = {
    'add': lambda a, b: a + b,
    'mul': lambda a, b: a * b,
    'out': lambda arg: print(arg),
    'pred': lambda a: a - 1,
    'gt': lambda a, b: a > b,
    'lt': lambda a, b: a < b,
}
